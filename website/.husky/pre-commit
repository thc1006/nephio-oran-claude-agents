#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Change to website directory for proper npm script execution
cd "$(dirname "$0")/.."

# Run lint-staged for staged files
npx lint-staged

# Run content validation for regression prevention
npm run validate:content
